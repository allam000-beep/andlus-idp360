# سجلّ الإصلاحات — الأحدث (الإصدار v=16)

هذا السجلّ يوثّق آخر جولة إصلاحات. الأرشيف `andlus-server-مُصلَح.tar.gz` يحوي هذه النسخة.

## إصلاحات هذه الجولة (v=16)

1. **بيانات الدورة الحضورية تظهر للمتابع الفني:** كانت تظهر للموظف فقط. الآن يراها المتابع عبر «عرض الخطة كاملة» (تُقرأ بمعرّف الموظف صاحب الخطة).

2. **شهادة حضور الدورة تظهر للموظف والمتابع:** كانت في لوحة مدير النظام فقط. الآن يظهر زرّ «🎓 تنزيل شهادة الحضور» ضمن بند الدورة عند تحقّق نسبة الحضور المطلوبة (> 75%) — للموظف وللمتابع الفني.

3. **إحكام اعتماد الخطة:** كل مسارات الاعتماد صارت تشترط أن يُنهي صاحب الخطة تخطيطه (isFinal) وأن يُعتمد المستوى السابق:
   - المتابع لا يعتمد خطة الموظف إلا بعد إنهائه التخطيط.
   - معتمِد القيادي (LeaderPlanApprovals) لا يرى الخطة للاعتماد إلا بعد إنهاء القيادي لها — (كان ثغرة، أُصلحت).
   - مدير الفرع لا يرى الخطة الجديدة إلا بعد اعتماد المتابع لها.

4. **التحديث شبه اللحظي عند النشر:** أُضيف تحديث تلقائي (كل 25 ثانية + عند عودة التركيز للصفحة) للوحات: المتابع الفني، مدير المرحلة، الموظف، مدير النظام. فتتزامن البيانات بين الحسابات خلال ثوانٍ دون إعادة تحميل يدوي.

## ما قبلها (مضمّن أيضاً في هذه النسخة)
- فرض دومين البريد @as.edu.sa + تحقّق رقم الهوية (10 أرقام) + الحقول الإلزامية في طلب فتح الحساب.
- إصلاح خطأ evalWinData + نافذة التقييم لكل المسمّيات + منع التقييم/التعديل عند الإغلاق.
- مصفوفة الأوزان المنفصلة (4 لوحات) + استعراض مدير النظام المنظّم للنتائج والخطط.
- حفظ is_final على الخادم + قراءة النتائج بعد الاعتماد + بيانات الدورة للموظف.

## النشر
1. ارفع الأرشيف كاملاً (الواجهة + الخادم). 2. الإصدار ?v=16. 3. أعد تشغيل الخادم (ترحيل is_final). 4. تحقّق: grep -c is_final controllers/idpsController.js = 4.
