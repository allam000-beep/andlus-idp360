# سجلّ إصلاحات — الجولة الثالثة (بعد مراجعة النظام المنشور)

إصلاحات على آخر نسخة أرسلتموها. الإصدار رُفع إلى **?v=8** (كسر الكاش).

## ✅ أُصلح واختُبر

### 1. نافذة التقييم لا تظهر حالتها للقياديين (المتابع/مدير المرحلة/مدير الفرع/أي قيادي)
**الجذر:** لوحات المتابع الفني ومدير المرحلة ومدير الفرع **لم تكن تحمّل نافذة التقييم إطلاقاً** — فتظهر الحالة خطأً (مغلق دائماً، أو لا تظهر). الإصلاح السابق كان للموظف فقط.
**الإصلاح:** أُضيف helper موحّد `branchWindowStatus(win, branch)`، وتُحمّل النافذة الآن في اللوحات الثلاث، مع:
- شارة حالة واضحة لكل فرع (🔓 مفتوح / 🔒 مغلق / ⌛ انتهت المدة).
- تعطيل زرّ التقييم عند إغلاق نافذة فرع الهدف (مع إتاحة العرض).

### 2. «حفظ وإغلاق التخطيط» غير نشط رغم اكتمال الحقول (كل المسمّيات)
**الجذر:** بعض البنود تُضاف عبر مخطّط مبسّط بلا حقل `mode` وبلا `cat` → كانت تُعامَل كإدخال يدوي وتتطلّب حقولاً غير ظاهرة (مصدر الاحتياج/الأسلوب) → تبقى «ناقصة» دائماً.
**الإصلاح:** يستنتج النظام وضع الصف تلقائياً (وجود مصدر/جدارة ⇒ تلقائي)، ولا يُلزم تغطية الفئات الثلاث إن لم تُحدَّد فئات أصلاً. الزرّ يُفعّل الآن عند اكتمال الحقول الفعلية.

### 3. استيراد بيانات المصادر يُرفض («لم يُعثر على بيانات صالحة»)
**الإصلاح:** أُضيف كشف ذكي للترويسة — الاستيراد يعمل الآن مع:
- ملفّ بترويسة عربية/إنجليزية،
- ملفّ **بلا ترويسة** (بيانات مباشرة)،
- **عمود واحد** (أسماء مصادر فقط)،
- فواصل متنوّعة (فاصلة، فاصلة منقوطة، Tab).

### 4. الدورات الحضورية الداخلية المخطّطة لم تُستدعَ في لوحة الإدارة
**الجذر:** اكتشاف الدورة الداخلية كان يفحص **نوع** المصدر لا **أسلوبه**.
**الإصلاح:** يفحص الآن أسلوب المصدر (`method`) من المكتبة ومن صف الخطة معاً — فالدورة ذات الأسلوب «داخلي حضوري أو عن بعد» تظهر في «إدارة الدورات الحضورية الداخلية».

## 🟡 يعتمد على البيانات
- **الفروع التابعة لمدير الفرع:** الآلية سليمة برمجياً (تظهر القائمة متى أُسندت عدّة فروع للمدير). إن ظهرت المشكلة رغم الإسناد بعد رفع هذه النسخة، فهي مزامنة تحتاج فحصاً إضافياً.

## 🔧 تشغيلي
- **البريد (نسيت كلمة السر):** يحتاج `npm install nodemailer` + ضبط SMTP + `APP_BASE_URL`.

## خطوات النشر
1. ارفع الأرشيف كاملاً (الواجهة **والخادم** معاً).
2. الإصدار رُفع إلى **?v=8** — تأكّد أن المنشور يعرض v8.
3. أعد تشغيل الخادم (لتطبيق ترحيل قاعدة البيانات).
4. تحقّق قبل النشر: `grep -c "التقييم مغلق" public/app.bundle.js` > 0.
