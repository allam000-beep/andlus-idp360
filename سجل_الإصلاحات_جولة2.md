# سجلّ إصلاحات جولة المراجعة (7 أغسطس)

إصلاحات على آخر نسخة، بعد ظهور ملاحظات أقلّ عقب النشر الصحيح.

## ✅ أُصلح واختُبر

1. **أسلوب المصدر:** قائمة الأسلوب في مكتبة المصادر أصبحت: «داخلي حضوري أو عن بعد / خارجي حضوري أو عن بعد / إلكتروني / قراءة». ومعها وُوئم اكتشاف الدورة الداخلية فتُستدعى في لوحة إدارة الدورات.

2. **اعتماد نتائج المرحلة:** الزرّ معطّل حتى يكتمل تقييم جميع الموظفين (يظهر «⏳ بانتظار اكتمال التقييم»).

3. **استيراد بيانات المصادر:** يقبل الآن ترويسات عربية متعددة (اسم المصدر/البرنامج/العنوان...) وفواصل متنوّعة (فاصلة، فاصلة منقوطة، Tab)، ويأخذ أوّل عمود غير فارغ للاسم إن لزم.

4. **حقول ربط الجدارات بالمصادر:** وُسّعت (أسماء كاملة بلا اقتصاص، مساحة أكبر).

5. **حقول بنود الجدارات:** وُسّع حقل الإدخال.

6. **المتابع الفني (المشرف المختص) يظهر كوكيل:** أصبح النموذج الافتراضي للمتابع الفني هو **المشرف المختص** (ذاتي/زملاء تخصص/متابع فني/مدير مباشر). لا يُعامَل كوكيل إلا إن حُدّد نوعه «وكيل (كمتابع)» صراحةً. يحلّ المشكلة حتى لو أُنشئ الحساب بلا نوع فرعي.

7. **مدير المرحلة يرى المشرف المختص كقيادي تابع:** صُحّح معتمِد خطة المشرف المختص → **المشرف التعليمي** (الامتداد الفني التعليمي)، لا مدير المرحلة — حتى بلا نوع فرعي.

8. **اعتماد الخطة نشط رغم الحفظ المؤقت:** زرّ اعتماد المتابع الفني معطّل حتى يُنهي الموظف التخطيط («حفظ وإغلاق»)، مع رسالة توضيحية.

9. **حفظ وإغلاق التخطيط غير نشط رغم الاكتمال:** كان يتطلّب «مصدر الاحتياج» لكل الصفوف، بينما الصفوف من المكتبة (الوضع التلقائي) لا تحويه. صار التحقّق **حسب وضع الصف**: التلقائي يحتاج (الجدارة/المصدر/التاريخ/طريقة القياس)، واليدوي يحتاج الحقول الكاملة.

10. **عبارة «يُفضّل الانتهاء من الدورات خلال الفصل الأول»:** أُضيفت لمخطّط الموظف (كانت غائبة).

11. **عرض خطة مدير المرحلة تُظهر بطاقة تقييم بها شهادة:** الآن «عرض» خطة القيادي التابع يفتح **خطة التطوّر** في وضع القراءة، لا بطاقة التقييم.

12. **بدء التقييم الذاتي رغم إغلاق تقييم الفرع:** عُطّل زرّ «ابدأ تقييمي الذاتي» حين تكون نافذة تقييم الفرع مغلقة.

## 🟡 تعتمد على البيانات (الآلية سليمة برمجياً)

- **الفروع التابعة لمدير الفرع / مدراء المراحل التابعون:** النظام يعرضهم إذا أُسندت للمدير عدّة فروع (`branches`) عند إنشاء حسابه عبر أداة الاختيار المتعدّد في شاشة المستخدمين. إن أُسند فرع واحد، يظهر فرع واحد — سلوك صحيح.
- **طلبات فتح الحساب المعلّقة:** منطق الاعتماد يُعيد القراءة من الخادم بشكل صحيح.

## 🔧 تشغيلي

- **البريد (نسيت كلمة السر):** يحتاج `npm install nodemailer` + ضبط SMTP + `APP_BASE_URL` (راجع دليل التسليم).

## عند النشر
1. ارفع الأرشيف كاملاً (`andlus-server-مُصلَح.tar.gz`).
2. رقم الإصدار رُفع إلى **?v=7** في index.html (كسر الكاش) — تأكّد أن المنشور يعرض v7.
3. أعد تشغيل الخادم (لتطبيق ترحيل قاعدة البيانات: عمود round وغيره).
4. للتأكّد قبل النشر: `grep -c myWin public/app.bundle.js` يجب أن يكون > 0.
