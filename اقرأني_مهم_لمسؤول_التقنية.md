# ⚠️ مهمّ جداً — لمسؤول التقنية قبل النشر

## الخلاصة في سطر
النسخة المنشورة حالياً **ليست** النسخة الأخيرة. الدليل قاطع بالأسفل. الحلّ: رفع هذا الأرشيف **كاملاً** (الواجهة + الخادم معاً).

---

## الدليل القاطع (كيف نتأكّد أيّ نسخة منشورة الآن)

تشخيص «كلود» الذي استُشير أشار إلى أن `controllers/authController.js` يُعيد صفّ المستخدم الخام هكذا:

```js
const { password_hash, ...safeUser } = user;
res.json({ token, user: safeUser });   // ← بلا فروع متعددة
```

**هذا الكود قديم.** في النسخة الأخيرة (هذا الأرشيف)، الملفّ نفسه يحوي دالة `enrichAuthUser` التي تجلب الفروع المتعددة:

```js
function enrichAuthUser(u) {
  const branches = db.prepare('SELECT branch FROM user_branches WHERE user_id = ?').all(u.id).map(r => r.branch);
  // ... stages, peerIds
  const { password_hash, role_subtype, ...safe } = u;
  return { ...safe, roleSubtype: ..., branches, stages, peerIds };
}
// login:  res.json({ token, user: enrichAuthUser(user) });
// me:     res.json({ user: enrichAuthUser(user) });
```

### الفحص الحاسم (٣٠ ثانية)
افتح **على الخادم المنشور** الملفّ `controllers/authController.js` وابحث عن كلمة `enrichAuthUser`:
- **إن لم توجد** → النسخة المنشورة قديمة (بلا إصلاحات الفروع/النافذة/الاستيراد/الدورات). هذا هو سبب بقاء الملاحظات.
- **إن وُجدت** → النسخة حديثة؛ راجعنا في حينه.

بناءً على تشخيص كلود (الذي رأى الكود القديم)، **النسخة المنشورة قديمة**.

### لماذا تحسّنت ملاحظة واحدة فقط (حفظ التخطيط)؟
لأن إصلاح حفظ التخطيط كان **جزئياً في نسخة وسيطة سابقة** رُفعت. أمّا إصلاحات هذه الجولة (النافذة، الفروع في authController، الاستيراد، الدورات) فكلّها في هذا الأرشيف ولم تصل بعد.

---

## طريقة النشر الصحيحة (نفس ما نجح سابقاً)

1. **احذف النسخة القديمة بالكامل** على الخادم/المستودع.
2. **ارفع محتويات هذا الأرشيف كاملاً** — ليس `app.bundle.js` وحده، بل:
   - `public/app.bundle.js` (الواجهة المبنيّة، 422KB)
   - `public/index.html` (فيه `?v=9`)
   - `public/api.js`
   - **`controllers/` كامل** (فيه authController المحدّث ← حرج للفروع)
   - **`db/` كامل** (فيه ترحيلات القاعدة)
   - بقيّة ملفّات الخادم
3. **أعد تشغيل الخادم** (لتطبيق ترحيل قاعدة البيانات).
4. تحقّق من النشر:
   - في المتصفّح (F12 → Network): `app.bundle.js?v=9` (لا أقلّ).
   - على الخادم: `grep -c enrichAuthUser controllers/authController.js` ← يجب أن يكون 3.
   - `grep -c "انتهت المدة" public/app.bundle.js` بعد فكّ الترميز، أو ابحث في المتصفّح عن `التقييم مغلق` داخل app.bundle.js.

---

## ملاحظة عن آلية البناء على Railway
إن كان Railway **يبني الـbundle تلقائياً** من `AndlusIDP360.jsx` عند كل نشر:
- تأكّد أن `AndlusIDP360.jsx` في المستودع هو **نسخة هذا الأرشيف** (410KB، فيها `myWin` و`branchWindowStatus`).
- تأكّد أن أمر البناء يشغّل `bash scripts/build_ui.sh ./AndlusIDP360.jsx`.

إن كان الـbundle **يُرفع جاهزاً** (لا يُبنى على Railway):
- ارفع `public/app.bundle.js` من هذا الأرشيف مباشرةً.

**الأهمّ:** الواجهة والخادم يجب أن يكونا من **نفس النسخة**. رفع الواجهة وحدها (دون controllers/db) يُبقي مشاكل الفروع والنافذة، لأنها تعتمد على الخادم المحدّث.
